# gcloud CLI 上傳原始碼（gcloud run deploy --source、gcloud builds submit 等）時
# 排除的檔案。跟 .gitignore 分開維護：這裡排除的是「不該進雲端建置環境」的東西，
# 範圍比 git 版控更嚴格（例如前端專案、文件、測試都跟後端部署無關）。

.git
.gitignore
.gcloudignore

# 本機開發專用，不該帶進雲端建置
.env
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
uv.lock
.claude/
.codex/
/AGENTS.md

# 文件與規格（只留在本機，不上雲端也不上版控，見 .gitignore）
docs/

# Flutter 前端專案，跟後端部署無關
mobile_app/

# 後端測試，不需要跟著部署到 Cloud Run
tests/

# 已棄用的 Unity+AR 原型
unity-flutter-bridge-demo/

# IDE/editor
.idea/
.vscode/

# 其他開發期產物
.scratch/
node_modules/

# 從 mattpocock/skills 拉下來的 agent skills（107 個檔案）。是開發期的 agent
# 設定，跟 Cloud Run 上跑的東西完全無關，不該進雲端建置環境。
#
# （.gitignore 也排除它，但理由不同：那邊是為了不讓 107 個非我們所寫的檔案
# 佔滿每一次 clone 與 diff。兩份清單各自維護，不要假設它們會一致。）
.agents/
