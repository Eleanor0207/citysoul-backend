.env
.scratch/
__pycache__/
*.pyc
.venv/
venv/
node_modules/
.dart_tool/
build/
.idea/
.vscode/
# 個人的 Claude Code 設定，含本機絕對路徑（citysoul-doc 的位置）
.claude/settings.local.json
# 個人的多資料夾 workspace 檔，路徑假設 citysoul-client 就在隔壁目錄
*.code-workspace

# Abandoned Unity+AR prototype (superseded by 2.5D Billboard, see project_dev.md) — too large for VCS
unity-flutter-bridge-demo/

# 從 mattpocock/skills 拉下來的 agent skills（107 個檔案）。內容不是我們寫的，
# 而且 skills-lock.json 已經記錄了來源與 hash——需要的人照 lock 檔還原即可，
# 不必讓它們佔滿每一次 clone 與 diff。
#
# ⚠️ 這跟下面例外放行的 docs/agents/ 是兩回事：那三個檔案是 CLAUDE.md 直接
# 引用的專案設定，必須撐過一次全新 clone。
.agents/

# Project docs/specs stay local only, not pushed to the remote.
# Exception: docs/agents/ holds the mattpocock-skills config that CLAUDE.md
# references (issue-tracker.md/triage-labels.md/domain.md) — needs to survive
# a fresh clone for the skills to keep working.
docs/*
!docs/agents/
!docs/agents/**

# 例外二：內容治理文件。理由與 docs/agents/ 相同——它必須撐得過一次全新 clone。
# B4（#11）與 B5（#19）的實作直接引用它當輸入，而它同時是人格卡上線的稽核軌跡
# （含審核者與審核日期）。一份簽核記錄如果只活在某個人的硬碟上，那就不是記錄。
!docs/content-governance/
!docs/content-governance/**

# 例外三：docs/dev-notes/ 記錄的決策與已知的規格／schema 落差在別處都沒有
# （例如 #19 的 AC 引用了 migration 0005 已經刪掉的欄位）。它一開始是本機的
# 暫存檔，也就是說只有一份未進版控的副本——離 `git clean` 弄丟只差一步。
!docs/dev-notes/
!docs/dev-notes/**

# Flutter 客戶端已於 SDD v2.1 汰換為 Unity（見 citysoul-client repo）。
# 原始碼保留在 git 歷史中，需要時 git show 4e19f0a:mobile_app/... 取回。
mobile_app/
